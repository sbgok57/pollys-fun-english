/* ============================================================
   🎙️ GERÇEK İNSAN SESİ HARİTASI (v6)
   VOICESET bildirgesi — mevcut insan sesi kayıtlarının slug listesi.
   Slug bu kümede varsa speak() audio/w-<slug>.mp3 dosyasını çalar;
   bulunamazsa tarayıcı TTS yedeğine döner.
   ============================================================ */
const VOICESET = new Set([
  'w-zztest'
]);
